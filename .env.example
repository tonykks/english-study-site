# Vertex AI (ADC) — uses Google Cloud Credit
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=global
VERTEX_GEMINI_MODEL=gemini-3.1-flash-lite

# OpenAI fallback is OFF by default. Set only if Owner explicitly allows:
# ALLOW_OPENAI_FALLBACK=1
# OPENAI_API_KEY=
